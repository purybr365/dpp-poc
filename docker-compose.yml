services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "file:./dev.db"
      NEXTAUTH_SECRET: "dpp-poc-secret-change-me-in-production"
      NEXTAUTH_URL: "http://localhost:3000"
